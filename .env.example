# better-auth session signing secret. Generate: openssl rand -base64 32
BETTER_AUTH_SECRET=
# Public base URL of the app (used for auth cookies/redirects in production)
BETTER_AUTH_URL=http://localhost:3000
